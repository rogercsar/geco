# Variáveis de ambiente do frontend (Vite)
# Em produção (Netlify), configure isto em Site Settings -> Environment

# Backend legado (se aplicável)
VITE_API_BASE_URL=

# Supabase (necessário para módulo de budgets)
# URL do projeto Supabase, ex: https://your-project-ref.supabase.co
VITE_SUPABASE_URL=
# Chave pública (anon) do projeto Supabase
VITE_SUPABASE_ANON_KEY=